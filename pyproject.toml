[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "airfoil-geometry"
version = "0.1.0"
description = "Herramientas para generación y manipulación de perfiles NACA"
authors = [{ name = "Tu Nombre" }]
requires-python = ">=3.9"

dependencies = [
    "numpy",
    "matplotlib",
    "cadquery"
]

